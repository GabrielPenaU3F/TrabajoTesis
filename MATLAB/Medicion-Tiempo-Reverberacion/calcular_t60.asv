function varargout = calcular_t60(s_db,fs)

   [t60, r_xy] = calcular_tx(s_db, fs, 60);
   if (nargout == 1)
       varargout = t60;
   elseif (nargout == 2)
       varargout = [t60, r_xy];
   end
        
    
    
    